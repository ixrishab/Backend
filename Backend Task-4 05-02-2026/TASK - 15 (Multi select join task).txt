TASK - 15 (Multi select join task)

Select * from students s join student_courses sc on s.id = sc.student_id join courses c on c.id = sc.course_id;

Select *,
	Case 
	when amount > 44500 then 'Good Salary'
	when amount < 44500 then 'Bad  Salary'
	End as Salary_Status
From Employees;
